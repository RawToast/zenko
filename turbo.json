{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "generate": {
      "outputs": ["src/**/*.gen.ts"],
      "inputs": ["resources/**/*.yaml", "generate.js"]
    },
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["**/*.{ts,tsx}"]
    },
    "//#lint": {},
    "//#lint:check": {},
    "//#format": {},
    "//#format:check": {},
    "check-types": {
      "dependsOn": ["generate", "^check-types"],
      "inputs": ["**/*.{ts,tsx}"]
    },
    "test": {
      "dependsOn": ["^test"]
    },
    "coverage": {
      "dependsOn": ["^coverage"],
      "cache": false
    }
  }
}
