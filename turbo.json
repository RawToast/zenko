{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "codegen": {
      "outputs": ["src/**/*.gen.ts"],
      "inputs": [
        "**/*.yaml",
        "!**/node_modules/**",
        "!**/dist/**",
        "!**/build/**",
        "generate.js"
      ],
      "cache": false
    },
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["**/*.{ts,tsx}"]
    },
    "//#lint": {},
    "//#lint:check": {},
    "//#format": {},
    "//#format:check": {},
    "check-types": {
      "dependsOn": ["codegen", "^check-types"],
      "inputs": ["**/*.{ts,tsx}"]
    },
    "test": {
      "dependsOn": ["^test"]
    },
    "coverage": {
      "dependsOn": ["^coverage"],
      "cache": false
    }
  }
}
