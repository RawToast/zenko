# Changesets

This folder stores the configuration and release notes used by Changesets.

If you're new to the workflow, run `bun run changeset` to add a change entry. Each entry is reviewed during releases to determine the next version and populate the changelog.
